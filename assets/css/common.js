import { StyleSheet } from "react-native";

export const Common = StyleSheet.create({
  container: {
    flex: 1,
  },

  viewHeight: {
    flex: 1,
  },

  row: {
    flexDirection: "row",
  },

  column: {
    flexDirection: "column",
  },
});
